[日本語](SERVING-NOTES-2026-09-20.ja.md) | [한국어](SERVING-NOTES-2026-09-20.ko.md) | [中文](SERVING-NOTES-2026-09-20.zh.md)

# Gemma 4 26B A4B on DGX Spark — サービング記録（2026-09-20）

> 数値はすべて同日の実測に由来する。旧測定系の値（prefix cache 有効での同一プロンプト再送、少数例の最大値を代表値にしたもの）は撤回済みで、本稿には含めない。

環境: DGX Spark 1 台（GB10 / sm_121 / 統合メモリ 128GB）、vLLM 0.28.0 を上流の Dockerfile から自前ビルド（`tenhkspark/vllm-gb10:v0.28.0-sm121`）、TP=1。測定はすべてノード上（localhost）から実行。

## 構成（A+γ8）

- 重み: NVIDIA 公式 NVFP4 チェックポイント（`nvidia/Gemma-4-26B-A4B-NVFP4`）に、`tie_word_embeddings` を外して lm_head を NVFP4 化した差分を足したもの
- MTP 投機デコード: `num_speculative_tokens=8`（γ=8）、draft は `google/gemma-4-26B-A4B-it-assistant`
- `--language-model-only`（vision tower を読み込まない）
- KV キャッシュ fp8
- `--gpu-memory-utilization 0.5` / `--max-num-seqs 32` / `--max-num-batched-tokens 8192` / `--max-model-len 32768` / prefix caching 無効

## 起動

### メモリ予約（最初に決める）

DGX Spark は GPU と OS が同じ 128GB の統合メモリを分け合う。`--gpu-memory-utilization` は「その何割を vLLM が起動時に先取りするか」の指定で、実際の負荷に関係なく、確保されたままになる。

既定は **0.5**（`gemma4.env` の `GEMMA4_GPU_UTIL=0.50`）。予約の内訳は重み 17.08 GiB（起動ログ実測）＋ MTP ドラフト 約 0.8GB ＋ KV キャッシュ ＋ 作業領域。0.5 での実測: 起動ログに `GPU KV cache size: 607,998 tokens` と出る（32K 窓換算で約 18.6 本分）。起動後の `free -g` は available 51〜52GB で、OS 側に余裕が残る。

util を上げるほど KV プールは増えるが、その分 OS 側は削られる。0.9 で起動した時は OS 側の空きが数 GB まで落ち、同居していたプロセスが落ちた（実測）。

- OS に余裕を残したい → 0.5（既定）
- 同時に保持する本数を増やしたい → 0.6 か 0.7

変え方は `gemma4.env` の `GEMMA4_GPU_UTIL` の 1 行だけ。変更後はコンテナの立て直しが必要（`./serve.sh down` → `up`、READY まで約 4 分）。

| GPU_UTIL | KV トークン | 32K 換算の同時本数 | OS 側の空き |
|---|---:|---:|---:|
| 0.5（既定） | 607,998 | 約 18.6 本 | 51〜52 GB |
| 0.6 | 837,957 | 約 25.6 本 | 40 GB |
| 0.7 | 1,011,401 | 約 30.9 本 | 27 GB |
| 0.9 | — | — | 数 GB。同居プロセスが落ちた |

起動ログで確認する 5 点（原文で揃ってから測定・利用する）:

1. `enable_prefix_caching=False`
2. `num_speculative_tokens=8`（γ=8 の MTP 投機）
3. `language_model_only=True`
4. `quantization=modelopt_fp4` と `FlashInferCutlassNvFp4` カーネルの選択
5. lm_head が量子化対象に保持されていること

サーバは `--host 0.0.0.0` で立つ（認証なし）。信頼できるネットワーク内で使うか、127.0.0.1 に留める。

## 速度

実務型プロンプト（日本語の架空業務文書 3 型: JSON 抽出・財務テンプレ穴埋め・監視ログ要約）30 文書 × 2 反復、温度 0、TTFT 込みの見出し速度（生成トークン数 ÷ 要求の全所要時間）、ノード上実行。

| 単発（C=1） | 実務型・自然 EOS | 短出力枠（出力 32 tok） |
|---|---:|---:|
| 公式 NVFP4 そのまま・投機 off | 28.8 tok/s | 28.0 tok/s |
| 公式 NVFP4 そのまま・γ8 | 100.5 tok/s | 未測定 |
| A+γ8 | **109.7 tok/s** | 未測定 |
| 投機 off 対照 | 35.8 tok/s | 未測定 |

投機デコードによる増速率: **3.06 倍**（109.7 / 35.8）。公式チェックポイントを
そのまま動かした場合も 28.8 → 100.5 の **3.49 倍**。lm_head を NVFP4 に
した効果は同じ γ8 同士で 100.5 → 109.7 の **+9.2%**、投機なし同士で
28.8 → 35.8 の **+24.3%**。つまりこの構成の速さは大部分が投機デコードに
由来し、lm_head の量子化はその上に 1 割を積む位置づけ。

並列は定常負荷（完了分を即補充して同時実行数を維持）で各並列度 60 秒。同一条件の 2 回の実行で一致した値。

| 同時実行数 | 合計 tok/s |
|---:|---:|
| C=8 | 496.7 |
| C=16 | 822.0 |
| C=32 | 1,080.8 |

本稿の速度・質の表はすべて util **0.6** で起動したサーバの測定値。既定を 0.5 にしても差し支えない根拠は 2 つある。単発は KV プール量に依存せず、0.5 の起動でも **104.5 tok/s** を実測している（0.6 の代表値と誤差内）。また C=32 の窓が要る KV は約 5 万トークンで、0.5 で確保される 607,998 トークンに対して十分に小さい。

## 長文（同一構成・既定値）

| 入力長 | C=1 (tok/s) | C=8 合計 (tok/s) | C=8 の要求 p50 |
|---:|---:|---:|---:|
| 1,024 | 109.7 | 未測定 | — |
| 8K | 30.6 | 62.9 | 14.9 s |
| 28K | 10.4 | 12.0 | 89.0 s |

KV プールは 837,957 トークン確保済みで、落ちる理由は KV 不足ではなく prefill 律速。実務上は長文を分割して短く投げる方が速い。

## 実務タスク（90 件）

日本語の実務タスク 90 件（SQL からのテーブル名抽出 db・財務テンプレ穴埋め finance・監視ログの要否判定 monitor、各 30 件）を温度 0 で流し、正しさ（passed）と処理量を計った。

| 同時実行数 | passed | 件/分 | 要求 p95 |
|---:|---:|---:|---:|
| C=1 | 84/90 | 7.59 | 13.98 s |
| C=8 | 82/90 | 35.90 | 20.01 s |
| C=32 | 80/90 | 83.40 | 29.88 s |

種別の内訳: **finance（表からの数値抽出）は全条件で 30/30**、monitor は C=1 で 30/30（「要対応」と答え続けるベースラインは 15/30）。db が最も落ちやすく、失敗はほぼ「余計なテーブルを挙げる」（table_extra）。この傾向は **BF16（量子化なし）でも同じく出る**（C=1 で BF16 20/30・本レシピ 24/30、BF16 側の処理量は 2.82 件/分・p95 29.36 s）ので、量子化由来ではない。

## 質

JGLUE valid + JMMLU。各 n=2,434（JCommonsenseQA は全件の 1,119）。同一問題・同一 seed・greedy のペアで BF16 と比較し、差分（pt）を記載 [*]。

| 指標 | n | Δ (pt) | 片側 95% 下限 (pt) |
|---|---:|---:|---:|
| JSQuAD EM | 2,434 | −0.66 | −1.17 |
| JSQuAD char-F1 | 2,434 | −0.17 | −0.41 |
| JNLI acc（1 回目） | 2,434 | −1.23 | −1.93 |
| JNLI acc（2 回目） | 2,434 | −1.48 | −2.14 |
| JCommonsenseQA acc | 1,119 | −0.36 | −1.07 |
| JMMLU acc | 2,434 | −0.70 | −1.60 |

判定: **5 指標とも点推定で −2pt 以内。JNLI は下限が −2pt をまたぐため、統計的非劣性は確認できていない。**

[*] 区間推定の正本はペア bootstrap（B=2,000・seed 固定・片側 95% 下限）。Δ は BF16 とのペア差の点推定。

### 独立セット

本評価は JGLUE valid に対するものなので、取りこぼしを疑い、構成の選択に使っていない JGLUE train split 由来の holdout 500 問/タスクで再確認した。JMMLU は train/valid の区別が構造上存在しないため holdout を作れず、独立確認は **JSQuAD・JNLI・JCommonsenseQA の 3 指標に留まる**。

| 指標 | Δ (pt) |
|---|---:|
| JSQuAD EM | +0.20 |
| JSQuAD char-F1 | −0.18 |
| JNLI acc | −1.60 |
| JCommonsenseQA acc | −0.20 |

## 再現（3 台・約 13 分）

- 同じ手順で、別の DGX Spark 3 台すべてに起動・smoke・ベンチが通った（現状確認→配置→起動→smoke→bench→撤収まで約 13 分）
- 上記の設定 5 点が 3 台とも起動ログの原文で一致
- ベンチ（C=1・3 回平均）: 99.3 / 105.6 / 102.7 tok/s。boot→READY は 241 秒
- 重みの manifest md5 `571932348835310ce77799f70a4e9814` が 3 台で一致（13 ファイル / 19,240,726,248 B）
- 重みは base に lm_head 差分を足した構造のため、base が既にあるノードへは 431MB の転送とハードリンクで約 1 分。全量コピーは不要
- 注意: temperature 0 でも台ごとに生成文が微妙に分岐した（重みの sha256 は一致）。「測り方」の出力の機差を参照

## 測り方

確定値はすべて次の手順で取得した。

- **prefix caching 無効**: 起動ログで `enable_prefix_caching=False` を原文確認してから測定。有効のまま同一系列を再送すると prefill がキャッシュから返り、実力より大きく見える。入力 2,048・出力 32 の条件では、同一プロンプトを再送した 2 回目が 20 倍以上に跳ねた（この値は実力ではないので本稿の表には載せていない）
- **ノード上実行**: ベンチは測定対象ノード上から `http://127.0.0.1:8890`（localhost）を叩く
- **実務型プロンプト**: 各文書 2 反復・温度 0・自然 EOS。代表値は 3 型の中央値の平均（最大値は使わない）
- **定常負荷**: 並列は完了分を即補充して同時実行数を維持する方式で、各並列度 60 秒
- **投機 off 対照**: 同じ重み・同じフラグで投機なしのアームを併走させ、増速率と出力の正しさを照合する
- **出力の機差**: temperature 0 でも台ごとに生成文が微妙に分岐することがある。重みの sha256 は一致するのに出力が分かれ、KV プール量の差に伴う数値誤差が原因と見られる。「同一重みならどの台でも同一応答」とは限らない

## コンテナのビルド

自前の Dockerfile ではなく、vLLM 上流リポジトリの `docker/Dockerfile` をそのままビルドしたもの。

```bash
DOCKER_BUILDKIT=1 docker build . \
    --tag wabi/vllm-gb10:v0.28.0-sm121 \
    --build-arg BUILD_BASE_IMAGE=pytorch/manylinuxaarch64-builder:cuda13.0 \
    --build-arg torch_cuda_arch_list=12.0 \
    --build-arg max_jobs=8 --build-arg nvcc_threads=2
```

- `torch_cuda_arch_list` は **12.0**（12.1 ではない）。GB10 は sm_121 だが 12.0 で動く
- 検証: `--entrypoint python3` で起動し、`torch.cuda.get_device_capability()` が `(12, 1)` を返すこと
- ビルド所要時間は実測未記録。重みは `huggingface-cli download tenhkspark/gemma-4-26B-A4B-NVFP4-lmhead`、コンテナは `docker pull tenhkspark/vllm-gb10:v0.28.0-sm121` で取得できる

## サイズ

重み 19.2GB ＋ ドラフト 0.8GB ＋ コンテナイメージ約 30GB ＝ 約 50GB。実行時は約 55GB。
GPU に載る量は起動ログの `Model loading took` で実測 17.08 GiB。

## 他の GPU で動かす場合（試算）

この節はすべて試算であり、実測は DGX Spark のみで行っている。

- Spark は統合メモリで OS と共有するため util を低め（既定 0.5）にする。ディスクリート GPU は VRAM を OS と共有しないため、0.85〜0.9 まで上げてよい
- 予約の内訳の目安: 重み 17.08 GiB（起動ログ実測）＋ MTP ドラフト 約 0.8GB ＋ KV キャッシュ
- 64GB の GPU: `--max-model-len 32768` で KV 約 35GB（32K 窓 × 約 20 本）が目安
- 32GB の GPU: `--max-model-len 8192`・`--max-num-seqs 8` から始める
- Spark 以外の GPU では実測していない

## 次の課題

### 全層 4bit ＋ 日本語校正（試した結果、採用せず）

本レシピの重みは attention と共有 MLP を BF16 のまま残している。そこも含めて
すべて NVFP4 にし、日本語の校正データ 365 件と誤差補償つきの量子化（GPTQ 系）で
作り直した版を実測した。

| 指標 | 本レシピ | 全層 4bit 版 |
|---|---:|---:|
| 単発 tok/s | 109.7 | **122.3（+11.5%）** |
| 並列 C=32 合計 tok/s | 1,080.8 | **586.5（約半分）** |
| JNLI のペア差 | −1.23 / −1.48 pt | −1.64 pt |
| 5 指標の点推定 | すべて −2pt 以内 | すべて −2pt 以内 |

**質は保てたが並列で大きく落ちたため採用しなかった。** 単発では速く、日本語の
生成も破綻しない。並列だけが遅い理由は分かっていない（単発は帯域律速、並列は
計算律速に入るため、という見方はあるが未検証）。

同じ測り方で並列の落ち込みを説明し、避けられるなら、単発の +11.5% を保ったまま
並列も取れる可能性がある。

## Tools used

Tools used — Claude Fable 5.1, GPT-6 (Astra), GLM-5.3-Flash. All code in this repository was written for this project.

## 謝辞・ライセンス

- Google（Gemma 4 本体と MTP ドラフト）、NVIDIA（NVFP4 チェックポイントと DGX Spark）、vLLM（サービングエンジン）に感謝する
- Gemma 4 のベースライセンスは Apache License 2.0
- 本稿の数値はすべてこのリポジトリ内の実測に由来する
